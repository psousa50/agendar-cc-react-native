import { variable } from "./../variables/platform"

export const theme = (_ = variable) => {
  const leftTheme = {
    flex: 1,
    alignSelf: "center",
    alignItems: "flex-start",
  }

  return leftTheme
}
