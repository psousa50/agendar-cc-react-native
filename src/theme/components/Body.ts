import { variable } from "./../variables/platform"

export const theme = (_ = variable) => {
  const bodyTheme = {
    flex: 1,
    alignItems: "center",
    alignSelf: "center",
  }

  return bodyTheme
}
